# pytest configuration
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Elasticsearch test configuration
[tool:pytest]
markers =
    slow: marks tests as slow
    elasticsearch: marks tests requiring elasticsearch
